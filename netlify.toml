[build]
  # Build command - converts markdown to JSON
  command = "npm run build"
  # Directory to publish
  publish = "."

# Redirect rules for admin panel
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
